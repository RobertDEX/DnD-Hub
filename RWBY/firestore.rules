rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // v8.2 LOCAL-ACCOUNT MODE
    // This restores the original direct Firestore sync used by the RWBY site.
    // The site account screen is for campaign convenience, NOT server security.
    // If you later enable Firebase Authentication, use firestore_auth.rules instead.

    match /campaigns/{campaignId} {
      allow read, write: if campaignId == 'rwby-campaign' || campaignId == 'rwby-campaign-2';
    }

    match /rwby-presence/{docId}     { allow read, write: if true; }
    match /rwby-presence-c2/{docId}  { allow read, write: if true; }
    match /rwby-backups/{docId}      { allow read, write: if true; }
    match /rwby-backups-c2/{docId}   { allow read, write: if true; }
    match /rwby-private/{docId}      { allow read, write: if true; }
    match /rwby-private-c2/{docId}   { allow read, write: if true; }
    match /rwby-meta/{docId}         { allow read, write: if true; }

    // Legacy migration source remains read-only.
    match /rwby-chars/{docId}        { allow read: if true; allow write: if false; }

    match /{document=**} { allow read, write: if false; }
  }
}
